public Class Gru {
    public static int isLegalNumber(int[] a, int bases)
    {
        int isLegalNumber = 1;

        for (int i = 0; i < a.length ; i++)
        {
            if (a[i] >= bases)
                isLegalNumber = 0;

        }
        return isLegalNumber;
    }
 }
